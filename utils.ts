/*
Array2D should be a type that represents a two dimensional array of some generic type

Add the appropriate type annotations to this definition
*/
export type Array2D = any;

/*
Creates an array of elements split into groups the length of size. If array 
can't be split evenly, the final chunk will be the remaining elements.

Add the appropriate type annotations to this method signature
*/
export function chunk(array: any, size: any): any {
  throw new Error('Not implemented yet.');
}

/*
Creates an array of grouped elements, the first of which contains the first 
elements of the given arrays, the second of which contains the second elements 
of the given arrays, and so on.

Add the appropriate type annotations to this method signature
*/
export function zip(...arrays: any[]): any {
  throw new Error('Not implemented yet.');
}